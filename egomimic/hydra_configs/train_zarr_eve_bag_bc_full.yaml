defaults:
  - model: hpt_bc_flow_eve_wrist_full
  - visualization: eve_cartesian
  - paths: default
  - trainer: ddp
  - debug: null
  - logger: wandb_offline
  - data: bag_eve_local_full
  - callbacks: checkpoints_full
  - data_schematic: eve_wrist
  - evaluator: eval_hpt
  - override hydra/launcher: submitit
  - _self_

name: ${oc.env:RUN_NAME,bag_BC_pg4_full}
description: ${oc.env:RUN_NAME,bag_BC_pg4_full}
ckpt_path: null
mode: train

hydra:
  run:
    dir: ${oc.env:EVE_HYDRA_RUN_DIR,./logs/${name}/${description}_${now:%Y-%m-%d_%H-%M-%S}}
  sweep:
    dir: ${oc.env:EVE_HYDRA_RUN_DIR,./logs/${name}/${description}_${now:%Y-%m-%d_%H-%M-%S}}

launch_params:
  gpus_per_node: 4
  nodes: 1

seed: 42

# Match OLD trainer/default.yaml: limit_*=100/64 batches per epoch (not full-set;
# OLD pipeline trained for 2000 epochs at this rate).
trainer:
  max_epochs: 2000
  min_epochs: 2000
  check_val_every_n_epoch: 100
  num_sanity_val_steps: 0
  limit_train_batches: 100
  limit_val_batches: 64
  precision: bf16-mixed

norm_stats:
  sample_frac: 0.1
  num_workers: 4
  save_cache_dir: ${hydra:runtime.output_dir}
  precomputed_norm_path: null
reject_outliers: false
