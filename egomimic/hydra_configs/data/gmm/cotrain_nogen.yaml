# @package data
# Same as gmm_cotrain but WITHOUT the generated cotrain data: the big-circle
# dir is new_circle_3 (not new_circle_3_plus_cotrain). The small-circle dir
# (new_circle_small__3) and everything else are unchanged.
defaults:
  - gmm/cotrain
  - _self_

train_datasets:
  pushshapes_sim:
    resolver:
      folder_path: /storage/project/r-dxu345-0/paphiwetsa3/datasets/new_circle_3
valid_datasets:
  pushshapes_sim:
    resolver:
      folder_path: /storage/project/r-dxu345-0/paphiwetsa3/datasets/new_circle_3
