_target_: egomimic.pl_utils.pl_data_utils.MultiDataModuleWrapper

# Embodiment collapse: mecka + scale are both `human_bimanual` now, so they can no
# longer be separate cotrain domains. Merged into ONE human_bimanual dataset that
# loads both labs, on the 144-D wrist-frame hand-keypoint action (see
# data/human.yaml). Scale exports carry obs_head_pose (sfs_to_egoverse_zarr.py
# writes it from the per-frame camera pose), so the merged human keymap keeps
# the default has_head_pose=true. fix_mecka_left_wrist is OFF: it would also
# flip the scale episodes' left ee_pose; split the labs into separate configs
# (mecka_all_6d, ...) when the prompt state must be exact.
train_datasets:
  human_bimanual:
    _target_: egomimic.rldb.zarr.zarr_dataset_multi.MultiDataset._from_resolver
    resolver:
      _target_: egomimic.rldb.zarr.zarr_dataset_multi.S3EpisodeResolver
      folder_path: ${paths.dataset_dir}
      key_map:
        _target_: egomimic.rldb.embodiment.human.Human.get_keymap
        keymap_mode: keypoints
        include_ee_pose: true
      transform_list:
        _target_: egomimic.rldb.embodiment.human.Human.get_transform_list
        mode: keypoints_wristframe_6d
        include_ee_pose: true
        # 18 -> 20-D ee_pose proprio (zero grip slots at 9/19) so the pi0.5
        # State: prompt bins align with the robot 20-D layout on every config.
        pad_proprio_gripper: true
        stride: 1
    filters:
      _target_: egomimic.rldb.filters.DatasetFilter
      filter_lambdas:
        - "lambda row: (row['lab'] == 'mecka' and row['task'] == 'fold_clothes') or (row['lab'] == 'scale' and row['task'] == 'flagship_fold_clothes')"
    mode: train

valid_datasets:
  human_bimanual:
    _target_: ${data.train_datasets.human_bimanual._target_}
    resolver: ${data.train_datasets.human_bimanual.resolver}
    filters: ${data.train_datasets.human_bimanual.filters}
    mode: valid

train_dataloader_params:
  human_bimanual:
    batch_size: 32
    num_workers: 10
valid_dataloader_params:
  human_bimanual:
    batch_size: 32
    num_workers: 10
