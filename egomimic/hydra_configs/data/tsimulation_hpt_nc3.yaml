# Same per-frame HPT loader as tsimulation_hpt.yaml, pointed at the new_circle_3
# dataset dir instead of circle/basic.
defaults:
  - tsimulation_hpt
  - _self_

train_datasets:
  pushshapes_sim:
    resolver:
      folder_path: /storage/project/r-dxu345-0/paphiwetsa3/datasets/new_circle_3
valid_datasets:
  pushshapes_sim:
    resolver:
      folder_path: /storage/project/r-dxu345-0/paphiwetsa3/datasets/new_circle_3
