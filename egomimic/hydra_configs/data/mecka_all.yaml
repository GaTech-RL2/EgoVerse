_target_: egomimic.pl_utils.pl_data_utils.MultiDataModuleWrapper


train_datasets:
  dataset1:
    _target_: egomimic.rldb.utils.S3RLDBDataset
    bucket_name: "rldb"
    mode: train
    sample_percent: 1.0
    embodiment: "mecka_bimanual"
    temp_root: "/mnt/efs/aseemdoriwala/.cache/egoverseS3Dataset"
    cache_root: "/mnt/efs/aseemdoriwala/.cache"
    local_files_only: true

valid_datasets:
  dataset1:
    _target_: egomimic.rldb.utils.S3RLDBDataset
    bucket_name: "rldb"
    mode: valid
    sample_percent: 1.0
    embodiment: "mecka_bimanual"
    temp_root: "/mnt/efs/aseemdoriwala/.cache/egoverseS3Dataset"
    cache_root: "/mnt/efs/aseemdoriwala/.cache"
    local_files_only: true

train_dataloader_params:
  dataset1:
    batch_size: 64  # Global 512 / 8 GPUs = 64 per GPU
    num_workers: 24

valid_dataloader_params:
  dataset1:
    batch_size: 64
    num_workers: 24
