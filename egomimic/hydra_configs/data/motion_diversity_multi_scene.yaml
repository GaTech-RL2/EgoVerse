_target_: egomimic.pl_utils.pl_data_utils.MultiDataModuleWrapper

# df columns: 
# ['episode_hash', 'operator', 'lab', 'num_frames', 'task',
# 'task_description', 'scene', 'objects', 'processed_path', 'mp4_path',
# 'is_deleted', 'embodiment', 'robot_name', 'is_eval', 'eval_score',
# 'eval_success']

# { _target_: egomimic.rldb.utils.S3RLDBDataset, bucket_name: "rldb", mode: total, embodiment: "aria_bimanual", local_files_only: True, 
#   temp_root: "/coc/cedarp-dxu345-0/datasets/egoverse/offline_eval_diversity",
#   filters: {operator: "Liqian", lab: "rl2", scene: "1", task: "fold clothes", recordings: null} }

train_datasets:
  dataset1: { _target_: egomimic.rldb.utils.S3RLDBDataset, bucket_name: "rldb", mode: total, embodiment: "aria_bimanual", local_files_only: True, 
              temp_root: "/coc/cedarp-dxu345-0/datasets/egoverse/offline_eval_diversity",
              filters: {operator: "Baoyu", lab: "rl2", scene: "8", task: "fold clothes", recordings: null} }

valid_datasets:
  dataset1: { _target_: egomimic.rldb.utils.S3RLDBDataset, bucket_name: "rldb", mode: total, embodiment: "aria_bimanual", local_files_only: True, 
              temp_root: "/coc/cedarp-dxu345-0/datasets/egoverse/offline_eval_diversity",
              filters: {operator: "Baoyu", lab: "rl2", scene: "8", task: "fold clothes", recordings: null} }

train_dataloader_params:
  dataset1:
    batch_size: 32
    num_workers: 10

valid_dataloader_params:
  dataset1:
    batch_size: 32
    num_workers: 10