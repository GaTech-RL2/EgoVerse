# Same per-frame HPT loader as tsimulation_hpt.yaml, pointed at the
# pushshapes_paper dataset dir instead of circle/basic.
defaults:
  - tsimulation_hpt
  - _self_

train_datasets:
  pushshapes_sim:
    resolver:
      folder_path: /storage/project/r-dxu345-0/paphiwetsa3/datasets/pushshapes_paper
valid_datasets:
  pushshapes_sim:
    resolver:
      folder_path: /storage/project/r-dxu345-0/paphiwetsa3/datasets/pushshapes_paper
