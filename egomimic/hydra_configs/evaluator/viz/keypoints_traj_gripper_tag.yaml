# Human cross-embodiment viz for the 140-D shared-head cotrain: draws the
# wrist + fingertip horizon trace (sliced from actions_140d down to 138-D)
# and overlays a top-right text tag showing the model's predicted gripper
# state at each frame (from 140-D slots [6, 76]).
human_bimanual:
  _target_: egomimic.rldb.embodiment.human.Human.viz_gt_preds
  _partial_: true
  image_key: observations.images.front_img_1
  action_key: actions_140d
  mode: keypoints_traj_gripper_tag
