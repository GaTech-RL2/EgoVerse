# Stage-tree H-Net pushshapes BASELINE: 128-dim AdaLN stage tree
# (EncoderDecoderStage -> ChunkerStage ratio 8 -> ComputeStage), RoPE + dropout
# backbone, F22 stability recipe ON, warmup-cosine LR. All payload lives in the
# abstract base; this baseline adds NO delta.
# extends _hnet_pushshapes_stage_common
defaults:
  - _hnet_pushshapes_stage_common
  - _self_
