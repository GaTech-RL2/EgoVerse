_target_: egomimic.pl_utils.pl_model.ModelWrapper
robomimic_model:
  _target_: egomimic.algo.hpt.HPT
  data_schematic: _${data.dataset.data_schematic}
  # camera_transforms:
  #   rby1: # TODO: check whether we need camera transform
  #     _target_: egomimic.utils.egomimicUtils.CameraTransforms
  #     intrinsics_key: "base" # change to base_half if using half res 
  #     extrinsics_key: "x5Dec13_2"
  
  diffusion: true 
  6dof: true

  ac_keys:
    rby1: "actions"

  # kinematics_solver:
  #   _target_: egomimic.robot.eva.eva_kinematics.EvaMinkKinematicsSolver
  #   model_path: /coc/flash7/rpunamiya6/Projects/EgoVerse/egomimic/resources/model_x5.xml

  trunk:
    embed_dim: 256
    num_blocks: 16
    num_heads: 8
    token_postprocessing: "action_token"
    observation_horizon: 1
    action_horizon: 10
    no_trunk: false
    use_domain_embedding: true
    drop_path: 0.1
    weight_init_style: "pytorch"
    proprio_dropout_p: 0.0  # set > 0 to drop proprio input during training (use learned null token)
  
  multitask: false
  pretrained: false
  pretrained_checkpoint: "" # TODO
  reverse_kl_samples: 8
  
  domains: ["rby1"]

  stem_specs:
    rby1:
      state_robot0_joint_pos:
        _target_: egomimic.models.hpt_nets.MLPPolicyStem
        input_dim: 26
        output_dim: 256
        widths: [256]
        specs:
          random_horizon_masking: false
          proprio_dropout_p: 0.0  # set > 0 to drop this proprio key during training
          noise_std: 0.0  # set > 0 to add Gaussian noise in normalized space
          cross_attn:
            crossattn_latent: 16
            crossattn_heads: 8
            crossattn_dim_head: 64
            crossattn_modality_dropout: 0.1
            modality_embed_dim: 256

  head_specs:
    rby1: 
      _target_: egomimic.models.fm_policy.FMPolicy
      action_horizon: 10 # determined by delta_timestamps in data config
      num_inference_steps: 50
      pooling: null
      time_dist: "beta"
      infer_ac_dims:
        rby1: 49
      model:
        _target_: egomimic.models.denoising_nets.CrossTransformer
        nblocks: 6
        cond_dim: 256
        hidden_dim: 128
        act_dim: 49
        act_seq: 10
        n_heads: 4
        dropout: 0.1
        mlp_layers: 4
        mlp_ratio: 4

  encoder_specs: {}

optimizer:
  _target_: torch.optim.AdamW
  _partial_: true
  lr: 1e-4
  weight_decay: 0.0001

scheduler:
  _target_: torch.optim.lr_scheduler.CosineAnnealingLR
  _partial_: true
  T_max: 1400
  eta_min: 1e-5
