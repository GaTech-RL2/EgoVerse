defaults:
  - hpt_bc_keypoints_constlr

# Arc-tokenized keypoints at constant lr: sequence is M+1 = 31 rows
# (30 waypoints + 1 velocity token) of the 138-dim layout.
robomimic_model:
  head_specs:
    human_bimanual:
      action_horizon: 31
      model:
        act_seq: 31
