# HPT flow-matching BC on human data (aria, stored as human_bimanual) with
# the 144-D wrist-frame hand-keypoint action (data/aria.yaml); identical to
# hpt_bc_flow_human.yaml apart from this note.
defaults:
  - hpt_bc_flow_human
  - _self_
