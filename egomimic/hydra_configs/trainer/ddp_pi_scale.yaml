defaults:
  - default

strategy: ddp_find_unused_parameters_true

accelerator: gpu
devices: ${eval:'${launch_params.gpus_per_node} * ${launch_params.nodes}'}
num_nodes: ${launch_params.nodes}
sync_batchnorm: True

max_steps: 31000
max_epochs: -1
limit_train_batches: 1.0
val_check_interval: 3100
num_sanity_val_steps: 0
