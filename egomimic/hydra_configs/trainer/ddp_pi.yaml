# Same as ddp but with find_unused_parameters (for models like pi0.5 whose
# action expert has params that don't receive grad on every step).
defaults:
  - ddp

strategy: ddp_find_unused_parameters_true
