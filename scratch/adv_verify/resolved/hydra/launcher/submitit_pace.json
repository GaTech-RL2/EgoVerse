{
  "_target_": "hydra_plugins.hydra_submitit_launcher.submitit_launcher.SlurmLauncher",
  "account": "gts-dxu345-rl2",
  "additional_parameters": {
    "requeue": true
  },
  "cpus_per_task": 8,
  "defaults": [
    "submitit_slurm"
  ],
  "gres": "gpu:h200:${eval:'${launch_params.gpus_per_node} * ${launch_params.nodes}'}",
  "mem_per_gpu": "250G",
  "name": "${hydra.job.name}",
  "nodes": "${launch_params.nodes}",
  "partition": "gpu-h200",
  "qos": "inferno",
  "tasks_per_node": "${launch_params.gpus_per_node}",
  "timeout_min": 2880
}
